spring:
  application:
    name: organizationservice
  profiles:
    active: dev
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
#      label: master // change it when your props are in different branch
management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 9090